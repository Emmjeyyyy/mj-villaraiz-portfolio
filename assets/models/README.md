# Raw 3D Assets

Place your raw `.glb` and `.gltf` files in this directory.

Run `npm run optimize:assets` to process these files. The optimized versions will be saved to `public/models/`.

### Supported Formats
- Binary glTF (`.glb`)
- glTF JSON (`.gltf`)
